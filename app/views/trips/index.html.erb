<div class="show-grid">
  <div class="show-grid-1">

    <div class="indent-box">
      <h1>My Trips</h1>
      <ul style="list-style: none;">
        <% @mytrips.each do |trip| %>
          <li>
            <a href="/trips/<%= trip.id%>"><%= trip.name %></a>  | Duration: <%= trip.duration %> hours
          </li>
        <% end %>
      </ul>
      <h1>All trips</h1>
      <ul style="list-style: none;">
        <% @alltrips.each do |trip| %>
          <li>
            <a href="/trips/<%= trip.id%>"><%= trip.name %></a>  | <%= trip.description %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
  <div class="show-grid-2">

    <%= form_with(model: Trip.new, local:true) do |f| %>
    <h1>Add a Trip</h1>
    <%= f.label "Name: ", class: 'form-label' %>
    <%= f.text_field :name %>
    <br>
    <%= f.label "Description: ", class: 'form-label' %>
    <%= f.text_field :description %>
    <br>
    <%= f.label "Start time", class: 'form-label' %>
    <%= f.datetime_select :start_time, :default => Time.now, ampm: true %>
    <br>
    <%= f.label "End time", class: 'form-label' %>
    <%= f.datetime_select :end_time, :default => Time.now, ampm: true %>
    <br>
    <%= f.submit %>
    <% end %>

  </div>
</div>
